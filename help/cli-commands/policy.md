# Policy

## Usage

`threatcode policy [<PATH_TO_POLICY_FILE>] [<OPTIONS>]`

## Description

The `threatcode policy` command displays the `.threatcode` policy file for a package.

## Argument: `<PATH_TO_POLICY_FILE>`

Manually pass a path to a `.threatcode` policy file.

## Debug

Use `-d` option to output the debug logs.
